import TransitRail from '@/src/views/TransitRail/TransitRail';

export const metadata = {
  title: 'Transit Rail | SNS Construction',
  description: 'Learn more about Transit Rail at SNS Construction.',
  alternates: {
    canonical: '/construction/transportation-infrastructure/transit-rail',
  },
};

export default function Page() {
  return <TransitRail />;
}
